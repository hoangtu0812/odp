image:
  repository: bsr-superset
  tag: 6.0.0
  pullPolicy: IfNotPresent
postgresql:
  enabled: false
redis:
  enabled: false
resources:
  requests: { cpu: 250m, memory: 512Mi }
  limits: { cpu: "1", memory: 1Gi }
